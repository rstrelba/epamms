# 🚀 Development Projects Backup Script

Мощный PowerShell скрипт для инкрементального бэкапа проектов разработки с автоматической очисткой Flutter проектов.

## ✨ Возможности

- 📁 **Инкрементальное копирование** - копирует только измененные файлы
- 🧹 **Автоматическая очистка Flutter проектов** - выполняет `flutter clean` перед бэкапом
- ⚙️ **Настраиваемые папки** - легко добавить новые папки для бэкапа
- 🚀 **Многопоточное копирование** - использует 8 потоков для быстрого копирования
- 📝 **Подробное логирование** - создает детальный лог операций
- 🔍 **Режим тестирования** - проверка без фактического копирования
- 🎯 **Умные исключения** - автоматически исключает временные файлы и папки

## 🎮 Использование

### Базовое использование
```powershell
.\backup_dev.ps1
```

### Дополнительные опции
```powershell
# Тестовый запуск без изменений
.\backup_dev.ps1 -DryRun

# Подробный вывод
.\backup_dev.ps1 -Verbose

# Бэкап только определенных папок
.\backup_dev.ps1 -FoldersToBackup @("Projects", "WWW", "MyNewFolder")

# Использование другого диска для бэкапа
.\backup_dev.ps1 -BackupRoot "E:\"

# Изменение исходной папки
.\backup_dev.ps1 -SourceRoot "C:\MyDev"
```

## 📋 Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `SourceRoot` | Корневая папка источника | `D:\DEV` |
| `BackupRoot` | Корневая папка бэкапа | `G:\` |
| `FoldersToBackup` | Массив папок для бэкапа | `@("Projects", "WWW")` |
| `DryRun` | Тестовый режим | `$false` |
| `Verbose` | Подробный вывод | `$false` |

## 🚫 Автоматические исключения

Скрипт автоматически исключает:

### Папки:
- `node_modules`
- `.git`
- `build`
- `.dart_tool`
- `.pub-cache`
- `bin`, `obj`
- `.vs`, `.vscode`
- `target`, `dist`, `out`

### Файлы:
- `*.tmp`, `*.temp`
- `*.log`, `*.cache`
- `*.lock`
- `Thumbs.db`, `.DS_Store`

## 🔧 Добавление новых папок

Чтобы добавить новые папки для бэкапа, измени массив `$DefaultFolders` в скрипте:

```powershell
$DefaultFolders = @("Projects", "WWW", "YourNewFolder")
```

Или используй параметр при запуске:

```powershell
.\backup_dev.ps1 -FoldersToBackup @("Projects", "WWW", "Mobile", "WebApps")
```

## 📊 Коды выхода Robocopy

- **0-7**: Успешное выполнение
- **8+**: Ошибки копирования

## 📝 Логирование

Скрипт создает файл `backup_log.txt` с подробной информацией о процессе копирования.

## 🎯 Особенности для Flutter проектов

Скрипт автоматически:
1. Находит все Flutter проекты (по наличию `pubspec.yaml` с `flutter:`)
2. Выполняет `flutter clean` перед бэкапом
3. Исключает папки `.dart_tool` и `build`

## 🚀 Производительность

- Многопоточное копирование (8 потоков)
- Зеркальное копирование с удалением устаревших файлов
- Возобновляемое копирование при сбоях
- 3 попытки повтора при ошибках

---

*Создано твоим гениальным ассистентом! 🎉*
